# ─────────────────────────────────────────────────────────────────────────────
# IMPORTANT: This app requires Python 3.11.
# Add a file named  .python-version  in your repo root containing just:  3.11
# Streamlit Cloud reads that file and provisions the correct interpreter.
# mediapipe does NOT work on Python 3.12+ (OSError loading shared libraries).
# ─────────────────────────────────────────────────────────────────────────────

# Core — works on Streamlit Cloud with Python 3.11
streamlit>=1.35.0
opencv-python-headless>=4.9.0

# Pin mediapipe to 0.10.14 — last release with mp.solutions.holistic
# on Python 3.11 (Linux x86_64).  Do NOT upgrade without testing.
mediapipe==0.10.14

numpy>=1.26.0,<2.0.0
pydantic>=2.0.0
google-genai>=0.8.0
python-dotenv>=1.0.0

# ─────────────────────────────────────────────────────────────────────────────
# The packages below are NOT available on Streamlit Cloud.
# Tab 2 (full pipeline) only works when running locally.
# To run locally, install these manually:
#   pip install "moviepy==1.0.3"
#   pip install camel-tools
#   camel-data -i all
# ─────────────────────────────────────────────────────────────────────────────
